import { MarkAssemble } from "@/components/marketing/mark-assemble";

/** Homepage §5a — facade mark (contrast beat between outcomes and stories). */
export function HomeContrast() {
  return (
    <section aria-label="Hansala confirmation mark">
      <MarkAssemble />
    </section>
  );
}
