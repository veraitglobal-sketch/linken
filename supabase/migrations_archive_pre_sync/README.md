# Archived local-only migration files (2026-07-20 sync)

These timestamps were never on remote. Equivalent SQL was applied via MCP under
different remote versions (now present as local files pulled from
`schema_migrations.statements`).

Do NOT re-apply. See sibling files in `supabase/migrations/` with remote versions.
